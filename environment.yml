name: tfenn
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - setuptools>=77
  - wheel
  - numpy>=1.23
  - scipy>=1.9
  - pip:
      - --extra-index-url=https://download.pytorch.org/whl/cpu
      - pytest>=8,<10
      - torch==2.13.0+cpu
