[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "TFENN"
version = "0.1.0"
description = "Shared tensor mathematics and typed covariant network components"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.23",
    "torch>=2.0",
]

[project.optional-dependencies]
test = ["pytest>=8,<10"]
experiment = ["comet_ml>=3.58,<4"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["TFENN*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
